import { extendTheme } from '@chakra-ui/react';

// Define your custom theme
const theme = extendTheme({
    colors: {
        navyBlue: '#002F6C',
        goldenYellow: '#FFD700',
        darkGoldenrod: '#DAA520',
    },
    // Add other customizations if needed
});

export default theme;
